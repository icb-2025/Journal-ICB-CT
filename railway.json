{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && cp .env .env.production || true && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:8080 -t public"
  }
}
