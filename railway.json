{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "nvm install 20 && npm install && npm run build && composer install --no-dev --optimize-autoloader && php artisan migrate --force"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=8080"
  }
}
