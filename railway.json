{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cp .env .env.production && composer install --no-dev --optimize-autoloader && php artisan config:clear && php artisan cache:clear && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:8080 -t public"
  }
}
